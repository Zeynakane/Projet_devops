pipeline {
    agent any

    

    stages {
        stage("Build de l'image de l'application") {
             steps {
		
                sh 'docker-compose up'
                echo "Building application image"
             }
        }
	    
      
}
