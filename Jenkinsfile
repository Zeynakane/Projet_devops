pipeline {
    agent any

    stages {
        stage('Build  Image appli') {
            steps {
             
                    sh 'docker compose up -d'
                
            }
        }
       
}
